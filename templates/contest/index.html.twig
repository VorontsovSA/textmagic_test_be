{% extends 'base.html.twig' %}

{% block title %}TextMagic homepage{% endblock %}

{% block body %}
    <div class="px-4 py-5 my-5 text-center">
        <h1 class="display-5 fw-bold text-body-emphasis">TextMagic Contest</h1>
        <div class="col-lg-6 mx-auto">
            <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
                <a href="{{ url('contest') }}" class="btn btn-primary btn-lg px-4 gap-3">{{ is_contest_started ? 'Continue' : 'Start' }}</a>
            </div>
        </div>
    </div>
{% endblock %}
